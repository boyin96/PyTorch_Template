# @package _global_
data_loader:
  _target_: data_loader.data_loader.get_data_loaders
  data_dir: data/fashion_mnist
  batch_size: ${batch_size}
  shuffle: true
  validation_split: 0.1
  num_workers: ${cpus}
  training: False
